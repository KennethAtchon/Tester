I want to make my own desktop app (maybe electron, whicher is the easiest to start). What the app does is makes test forms.
I want to test my understandable of topics, it loads in a json file(which has all the tests) and then the user takes the tests and the test is graded by an AI and shown to the user.
It wont be graded automatically by the AI, the output of the test from the user will be exported to a markdown file and people can point AIs to that markdown to review and create a results markdown file with explanations.

